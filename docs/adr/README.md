# Architecture Decision Records

ADRs are reserved for cross-cutting decisions that change the public API,
package boundaries, or the build/packaging pipeline.

For current decisions, see the issue backlog and `AGENTS.md`.
